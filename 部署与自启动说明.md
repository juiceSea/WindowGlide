# WindowGlide：另一台 Windows 电脑的部署与自启动说明

适用版本：0.4.5。本文可交给目标电脑上的 Agent 执行。使用目标电脑的实际路径，先验证手动启动，再添加当前用户登录后的后台自启动。

## 执行范围和前提

- 主要验证环境：Windows 11、64 位 Python 3.14。要求 64 位 Python 3.11 或更新版本，无第三方运行依赖。其他系统与架构需另行验证。
- 使用普通用户权限，不勾选管理员运行；高权限应用窗口可能无法操作。
- 只创建当前用户的 Startup 快捷方式，不修改注册表、服务或其他启动项。这是登录桌面后的启动，不是未登录时运行的系统服务。
- 触摸板手势由 Windows 设置发送 Ctrl + Win + Alt + J/K/M，另一台电脑需自行配置，不随文件迁移。
- 旧 TouchpadGestures.ahk 或同功能工具应退出，避免重复响应。不要结束所有 AutoHotkey/Python 进程，也不要删除旧脚本。
- `config.json` 的 `drag_modifier` 可选 `"Win"` 或 `"Alt"`，附带配置使用 Win，旧配置缺省使用 Alt。Win 模式需检查截图等软件是否占用了 Win＋鼠标拖动。
- `enable_shift_left_resize` 为 true 时，还可用所选修饰键＋Shift＋左键缩放。触摸板采用单指双击，第二次按住拖动；不修改双指右键及滚动。附带配置开启，旧配置缺省关闭，部署时检查 FancyZones 等 Shift 拖窗工具的兼容性。
- Agent 不应擅自注销或重启电脑，以免丢失用户正在编辑的内容。登录自启动需要用户方便时验收。

## 1. 部署文件

使用当前用户可写的固定目录，例如 `%LOCALAPPDATA%\WindowGlide` 或用户指定的 `D:\Tools\WindowGlide`，不要使用临时目录或普通用户不可写的 Program Files。

必须复制 `main.py`、完整的 `windowglide` 文件夹、`config.json`、`start_windowglide.cmd` 和 `stop_windowglide.cmd`。现有外观和快捷键配置随 config.json 迁移。建议同时复制 README 和本说明，需要自动测试时带上 tests。

不需要复制 logs、test-results、__pycache__、.venv。日志可能含私人窗口标题，不应随部署迁移。覆盖已有部署前确认它属于 WindowGlide，先退出实例；保留目标机已有配置，除非用户明确要求替换。

## 2. 确认 Python

在目标机 PowerShell 执行：

```powershell
python -c "import sys, struct; print(sys.executable); print(sys.version); print(struct.calcsize('P') * 8)"
```

最后一行必须为 64，版本至少 3.11。若 python 是商店占位程序或错误环境，先定位真实安装。未安装时从 [Python 官方网站](https://www.python.org/downloads/windows/) 安装 64 位版本，推荐 3.14 系列。安装后重新打开 PowerShell。

以下步骤使用完整路径，不依赖登录时的 PATH。先按目标机实际情况设置变量：

```powershell
$wgRoot = (Resolve-Path -LiteralPath 'D:\Tools\WindowGlide').Path
$wgPython = 'C:\目标机实际Python目录\python.exe'
$wgPythonw = Join-Path (Split-Path -Parent $wgPython) 'pythonw.exe'
if (!(Test-Path -LiteralPath $wgPythonw -PathType Leaf)) { throw '未找到 pythonw.exe' }
if (!(Test-Path -LiteralPath (Join-Path $wgRoot 'main.py') -PathType Leaf)) { throw '缺少 main.py' }
& $wgPython -c "import sys, struct; assert sys.version_info >= (3,11); assert struct.calcsize('P') == 8; print(sys.executable)"
if ($LASTEXITCODE -ne 0) { throw 'Python 环境验证失败' }
```

## 3. 先验证手动启动

先确认当前没有 WindowGlide 实例，否则烟雾测试因单实例保护直接退出，不能代表新部署启动成功。

```powershell
& $wgPython (Join-Path $wgRoot 'main.py') --smoke-seconds 2
if ($LASTEXITCODE -ne 0) { throw '启动检查失败，请查看终端和日志' }
```

输出应同时出现 READY 和 STOPPED，无异常；READY 的 drag_modifier 应与配置一致。这个检查不主动移动鼠标或操作应用窗口。之后正常启动，按配置的 Win 或 Alt 验证左键移动、右键缩放、J 最小化、K 恢复和 M 最大化。Win 模式还需验证拖动结束不弹开始菜单、下一次单按 Win 仍打开开始菜单，再按 Ctrl + Win + Alt + Q 退出。

## 4. 创建当前用户自启动快捷方式

只在前面检查通过后执行。脚本使用已验证的变量和系统 Startup 目录，不硬编码用户名。若已有快捷方式，先核对是否属于同一个部署，再决定保留或更新，不覆盖无关快捷方式。

```powershell
$wgStartup = [Environment]::GetFolderPath('Startup')
$wgLinkPath = Join-Path $wgStartup 'WindowGlide.lnk'
$wgShell = New-Object -ComObject WScript.Shell
if (Test-Path -LiteralPath $wgLinkPath) {
    $wgExisting = $wgShell.CreateShortcut($wgLinkPath)
    Write-Output ('已有目标: ' + $wgExisting.TargetPath)
    Write-Output ('已有参数: ' + $wgExisting.Arguments)
    throw '已有快捷方式，请核对归属和路径后保留或单独更新。'
}
$wgLink = $wgShell.CreateShortcut($wgLinkPath)
$wgLink.TargetPath = $wgPythonw
$wgLink.Arguments = '"' + (Join-Path $wgRoot 'main.py') + '"'
$wgLink.WorkingDirectory = $wgRoot
$wgLink.Description = 'WindowGlide 窗口移动、缩放与快捷键'
$wgLink.Save()
Write-Output $wgLinkPath
```

pythonw.exe 在后台运行，没有终端窗口。不要把 .cmd 启动器放入 Startup，它会显示终端；不要额外添加计划任务或第二个启动项。

手动方式：Win + R → shell:startup → 新建快捷方式，目标为 `"实际pythonw.exe路径" "实际部署目录\main.py"`，起始位置为部署目录。[微软说明](https://support.microsoft.com/zh-cn/windows/experience/startup-boot/configure-startup-applications-in-windows)

## 5. 验证并报告

- 双击新快捷方式，或执行 `Start-Process -FilePath $wgLinkPath -WindowStyle Hidden`。
- 检查最新的 `logs\windowglide.YYYY-MM-DD.NNNNNN.log`，应有本次启动时间的 READY，且 test_input=False；核对进程命令行指向本部署。没有托盘图标或程序窗口是正常的。
- 再试移动、缩放和快捷键；触摸板需要用户用真实设备验证。
- 用户方便时注销重新登录或重启，验证自动启动。尚未做时明确报告“快捷方式已验证，登录自启动待验收”。
- 交付时报告部署目录、Python 路径、快捷方式路径和验证结果。

## 6. 日常维护

| 动作 | 方法 |
|---|---|
| 退出当前实例 | Ctrl + Win + Alt + Q |
| 命令退出 | `& $wgPython (Join-Path $wgRoot 'main.py') --stop` |
| 修改配置 | 编辑 config.json，退出后重启 |
| 取消自启动 | Win + R → shell:startup → 仅删除 WindowGlide.lnk；当前实例需另行退出 |
| 排查启动失败 | 查看最新日期日志，或用 python.exe main.py 在终端启动 |

日志保留本机日期的今天及前 6 天，同时限制总量约 8 MB、单份约 2 MB，日志多时可能不足七天。启动时清理，运行期间每小时清理一次；换日、轮转或容量达到上限时也检查。等待期间线程休眠，不监视目录、不逐秒扫描。关闭期间不运行任务，下次启动补清理。

清理失败会重试；无法腾出空间时可能丢弃新日志，以免继续增长。旧版 windowglide.log 和 .1–.3 在新版本首次启动时迁移，保留期内记录会保留（受总量上限约束）；不清理其他文件。分享日志前检查私人窗口标题。

后台启动失败时，检查路径变化、配置 JSON、目录写权限和退出快捷键占用情况。移动程序目录或更新 Python 安装路径后需修改快捷方式。重复启动会退出新实例，不叠加 Hook。
